- id: repo-scan-detect-secrets
  name: Detect secrets
  description: Detects high entropy strings that are likely to be passwords.
  entry: --entrypoint detect-secrets donaldrich/function:detect-secrets
  args: ["scan"]
  # args: [--baseline, ci/secrets.baseline]
  language: docker_image
- id: repo-scan-yelp
  name: Detect secrets
  description: Detects high entropy strings that are likely to be passwords.
  entry: --entrypoint detect-secrets donaldrich/function:detect-secrets
  # args: ["scan"]
  # language: docker_image
- id: repo-secrets-yelp
  name: Detect secrets
  description: secrets high entropy strings that are likely to be passwords.
  entry: --secrets detect-secrets donaldrich/function:detect-secrets
  # args: ["scan"]
  # language: docker_image
