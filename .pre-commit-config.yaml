repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v3.2.0
    hooks:
      - id: check-added-large-files
      - id: check-executables-have-shebangs
      - id: check-yaml
        exclude: mkdocs.yml
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args: ["--fix=lf"]
  # - repo: https://github.com/prettier/prettier
  #   rev: 2.1.2
  #   hooks:
  #     - id: prettier
  #       verbose: true
  - repo: local
    hooks:
      - id: repo-policy-commitlint
        name: repo-policy-commitlint
        entry: --entrypoint commitlint donaldrich/function:semantic-release
        language: docker_image
        # args: ["--from=HEAD~1"]
        # verbose: true
        stages:
          - commit-msg
      # - id: lint-git-conform
      #   name: lint-git-conform
      #   entry: --entrypoint conform donaldrich/function:conform
      #   language: docker_image
      #   args: ["enforce"]
      #   # args: ["enforce", "--commit-msg-file"]
      #   pass_filenames: false
      # stages: [commit-msg]
      # - id: repo-semver-semantic-release
      #   name: repo-semver-semantic-release
      #   entry: --entrypoint semantic-release donaldrich/function:semantic-release
      #   language: docker_image
      #   args: ["--generate-notes", "false" ,"--dry-run"]
      #   pass_filenames: false
      #   verbose: true

  # - repo: https://github.com/talos-systems/conform
  #   rev: v0.1.0-alpha.20
  #   hooks:
  #     - id: conform
  #       stages:
  #         - commit-msg

  #     -   id: check-x
  #         name: Check X
  #         entry: ./run-all.sh
  #         language: script
