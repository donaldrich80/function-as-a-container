image:
  tag: commit
  context: ""
  dir: ""
  dockerfile: ""
commands:
  commitlint:
    prefix: commitlint
    help: -h
    version: -v
    github: conventional-changelog/commitlint
  commitizen:
    prefix: cz
    help: --help
    version: --version
    github: commitizen-tools/commitizen
    docs: https://commitizen-tools.github.io/commitizen
  git-conventional-commits:
    prefix: git-conventional-commits
    help: --help
    version: --version
    github: qoomon/git-conventional-commits
  conform:
    prefix: conform
    help: --help
    version: --version
    github: talos-systems/conform
    download: https://github.com/talos-systems/conform/releases/download/v0.1.0-alpha.19/conform-linux-amd64
extra: ""
