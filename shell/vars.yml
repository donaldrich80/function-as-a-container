image:
  tag: shell
  context: ""
  dir: ""
  dockerfile: ""
commands:
  shellcheck:
    prefix: shellcheck
    help: --help
    version: --version
    github: koalaman/shellcheck
  bashate:
    prefix: bashate
    help: --help
    version: --version
    github: openstack/bashate
  shfmt:
    prefix: shfmt
    help: -h
    version: -version
    github: mvdan/sh
    docker: mvdan/sh
    base: mvdan/shfmt
  shellharden:
    prefix: shellharden
    help: --help
    version: --version
    github: anordal/shellharden
  # argbash:
  #   prefix: argbash
  #   help: --help
  #   version: --version
  #   github: matejak/argbash
  #   docker: matejak/argbash
  #   site: https://argbash.io/generate
  #   base: matejak/argbash
extra: ""
