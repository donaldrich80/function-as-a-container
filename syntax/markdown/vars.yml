image:
  tag: markdown
  context: ""
  dir: ""
  dockerfile: ""
commands:
  markdown-magic:
    prefix: markdown
    help: --help
    version: --version
    github: DavidWells/markdown-magic
  markdown-pdf:
    prefix: markdown-pdf
    help: --help
    version: --version
    github: alanshaw/markdown-pdf
  markdownlint:
    prefix: markdownlint
    help: --help
    version: --version
    github: DavidAnson/markdownlint
  remark:
    prefix: bash
    help: --help
    version: --version
    github: remarkjs/remark-lint
    docs: https://remark.js.org
  # markdown-spellcheck:
  #   prefix: mdspell
  #   help: --help
  #   version: --version
  #   github: lukeapage/node-markdown-spellcheck
extra: ""
